<h1>Listing recurring_profile_line_items</h1>

<table>
  <tr>
    <th>Invoice</th>
    <th>Item</th>
    <th>Item name</th>
    <th>Item description</th>
    <th>Item unit cost</th>
    <th>Item quantity</th>
    <th>Tax 1</th>
    <th>Tax 2</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <% @recurring_profile_line_items.each do |recurring_profile_line_item| %>
      <tr>
        <td><%= recurring_profile_line_item.invoice_id %></td>
        <td><%= recurring_profile_line_item.item_id %></td>
        <td><%= recurring_profile_line_item.item_name %></td>
        <td><%= recurring_profile_line_item.item_description %></td>
        <td><%= recurring_profile_line_item.item_unit_cost %></td>
        <td><%= recurring_profile_line_item.item_quantity %></td>
        <td><%= recurring_profile_line_item.tax_1 %></td>
        <td><%= recurring_profile_line_item.tax_2 %></td>
        <td><%= link_to 'Show', recurring_profile_line_item %></td>
        <td><%= link_to 'Edit', edit_recurring_profile_line_item_path(recurring_profile_line_item) %></td>
        <td><%= link_to 'Destroy', recurring_profile_line_item, method: :delete, data: {confirm: 'Are you sure?'} %></td>
      </tr>
  <% end %>
</table>

<br/>

<%= link_to 'New Recurring profile line item', new_recurring_profile_line_item_path %>
